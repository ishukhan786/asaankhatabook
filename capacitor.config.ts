import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asaankhatabook.app',
  appName: 'Asaan Khata Book',
  webDir: 'dist'
};

export default config;
